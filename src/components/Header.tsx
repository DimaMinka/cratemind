import React from 'react';

/**
 * Header.tsx
 * Renders the top status bar containing the listening/paused status
 * and general processing statistics.
 */

export function Header(): React.JSX.Element {
  // TODO: Render Ink layout for Top status bar
  return <></>;
}
