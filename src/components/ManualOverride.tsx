import React from 'react';

/**
 * ManualOverride.tsx
 * Renders the right multi-select panel for manual vibe/folder overrides.
 * Uses Ink's useInput for custom checkbox toggling and cursor movement.
 */

export function ManualOverride(): React.JSX.Element {
  // TODO: Render multi-select folder checkboxes using useInput
  return <></>;
}
